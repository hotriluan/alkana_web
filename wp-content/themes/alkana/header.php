<?php
/**
 * header.php — WordPress theme header wrapper.
 *
 * WordPress's get_header() template tag loads this file. We delegate
 * to the actual template part so all markup lives in one place.
 *
 * @package Alkana
 */
get_template_part( 'template-parts/header' );
